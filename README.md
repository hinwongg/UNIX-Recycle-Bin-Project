# UNIX-Recycle-Bin-Project
UNIX has no recycle bin at the command line.  When you remove a file or directory, it is gone and cannot be restored. This project is to write a remove script and a restore script to provide users with a recycle bin which can be used to safely delete and restore files.
